import { Grid, Typography } from "@mui/material";

const Menu = () => {
  return (
    <Grid container>
      <Typography variant="h2" color="textPrimary">Play Menu</Typography>;

    </Grid>
  );
};

export default Menu;
